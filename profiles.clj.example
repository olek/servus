{:dev {:dependencies [[org.clojure/tools.namespace "0.2.11"]]
      :source-paths ["dev"]
      :repl-options {:init-ns user}
      :env {:foo "Bar"}}
:repl {:injections [(user/reset)
                    (user/hints)]}}
